name: LostPengu Code Agent

on:
  schedule:
    - cron: '*/30 * * * *'    # Her 30 dakikada bir
  workflow_dispatch:

jobs:
  code-agent:
    runs-on: ubuntu-latest

    steps:
      - name: LostPengu Code Agent - Status Log
        run: |
          echo "========================================"
          echo "🐧 LostPengu Code Agent is ACTIVE"
          echo "Time: $(date)"
          echo "Status: Working Successfully"
          echo "Action: Logging only (no changes)"
          echo "The colony is evolving..."
          echo "========================================"
          echo "Next run scheduled in 30 minutes."
        shell: bash
